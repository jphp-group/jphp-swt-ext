
name: jphp-swt-ext
version: 1.0.0+dev

plugins: [Hub, Doc, Gradle]

deps:
  jphp-runtime: '*'
  jphp-graphic-ext: '>=1.0.0'

hub:
  login: d.zayceff@gmail.com

gradle:
  providedDeps:
    - 'file:src-jvm/main/resources/swt/swt-win-x86.jar'

config:
  ignore: ['/sandbox/**', '/.idea/**', '/*.iml', '/.git/**', '/examples/**']

doc:
  url-prefix: 'https://github.com/jphp-compiler/jphp-swt-ext/blob/master/api-docs/'
  langs: {en: 'English', ru: 'Русский'}